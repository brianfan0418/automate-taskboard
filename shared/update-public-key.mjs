// ed25519 public key that verifies shared-folder update manifests.
// Placeholder: this public repository ships no key, so shared-folder updates are refused
// (INVALID_PUBLIC_KEY) until you generate your own pair with `npm run update:keygen`.
// That command writes the private key outside the repository and rewrites this file.
export const UPDATE_PUBLIC_KEY_PEM = "";
